PlaceIt.Views.GoogleMap = Backbone.View.extend({

  initialize: function() {
    this.markers = [];
    this.render();
  },

  render: function() {
  },

  addMarker: function(model) {
    // var view = new PlaceIt.Views.LocationMarker( {model: model} );
    // this.markers.push(view);
    // this.$el.append( view.$el );
  }

});
